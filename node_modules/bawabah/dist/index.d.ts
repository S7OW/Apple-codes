interface Credentials {
    appId: string;
    appSecret: string;
}
interface CaptureResponse {
    openId: string;
    displayname: string | null;
    avatar: string | null;
    method: {
        type: "email" | "whatsapp";
        value: string;
    };
}
declare class Bawabah {
    private credentials;
    private formatError;
    init({ appId, appSecret }: Credentials): void;
    capture(sessionId: string): Promise<CaptureResponse>;
}

export { Bawabah as default };
